# Baseflow 节点 Runtime v1 改造方案

## 1. 目标与范围

Baseflow 节点物料由 `baseflow-node-renderer` 在 iframe Realm 中动态加载。节点不是独立应用，需要通过 renderer 提供的公共模块复用 React、React DOM 和 `@baseflow/render-react`。

本方案只定义：

- 节点加载协议和 Runtime ABI；
- Runtime v1 的五个公共模块入口；
- 官方节点的默认构建约定；
- Runtime 的版本演进和发布边界。

平台不充当社区节点的包管理器，不建设通用 npm-to-ESM 转换、完整依赖图扫描或完备兼容性验证。社区节点的第三方依赖、外部 URL 和目标浏览器兼容性主要由节点开发者自行测试和保证。

以下内容明确留到后期专项解决，本轮不作为完成条件：

- 独立域部署、跨窗口消息鉴权和更严格的安全隔离；
- 真实浏览器、跨域、外部 ESM 和完整历史产物的系统性测试覆盖。

## 2. Runtime v1 契约

### 2.1 公共模块入口

Runtime v1 只提供以下五个 bare import：

```text
react
react/jsx-runtime
react-dom
react-dom/client
@baseflow/render-react
```

Import Map 只包含上述完整入口，不提供包名前缀映射。以下子路径不属于 Runtime v1：

```text
react/jsx-dev-runtime
react/compiler-runtime
react-dom/server
react-dom/server.browser
react-dom/static
react-dom/profiling
react-dom/test-utils
@baseflow/render-react/*
```

五个入口及其公开 API 属于 Runtime ABI。React、React DOM 中名称包含 `__` 的内部成员，以及实验性、编译器或 `unstable_*` API 即使当前门面能够导出，也不属于稳定兼容承诺。

renderer 通过 Import Map 提供同一套共享实例。只有通过五个官方入口引用的依赖，平台才承诺其单例关系。节点自行携带另一份 React、使用未登记子路径或通过外部 URL 引入其它 React 版本时，由节点开发者承担兼容风险。

### 2.2 当前实现版本

Runtime v1 当前基线为：

```text
react@19.2.8
react-dom@19.2.8
@baseflow/render-react@0.0.4
```

实际版本由 lockfile 和 shared 产物共同确定，`package.json` 中的 semver range 不表示运行时可以自动漂移。

共享依赖升级属于显式 Runtime 变更：

- 公开 API 和既有行为保持兼容时，可以继续实现 Runtime v1；
- 无法保持兼容时，新增 Runtime 版本并保留旧 Runtime，不要求历史节点重新构建。

构建工具不属于 ABI。升级 Vite、Rollup、plugin-react 或 TypeScript，只要最终产物继续满足 Runtime v1，就不要求历史节点重新构建。

### 2.3 节点模块格式

节点入口的最小要求：

- 使用目标浏览器可加载的标准 ESM；
- 入口文件为 `index.js`；
- 默认导出为 renderer 可挂载的 React Component；
- 需要平台 Runtime 时声明对应 `runtimeVersion`；
- 需要平台共享单例时只使用五个官方 bare import。

`mod.default ?? mod` 仅作为历史产物的 legacy 适配，不属于新节点标准。确认不存在非 default export 的历史发布产物后，可以删除该兼容分支。

除五个公共入口外，节点可以使用：

- 节点自身的相对或绝对 URL；
- 指向其它来源的完整 HTTP(S) URL；
- 静态 `import` 或动态 `import()`；
- 被节点构建工具打入本地产物的其它依赖。

平台不代理、改写或验证这些依赖。未登记的其它 bare import 由浏览器按标准模块解析规则报错。

### 2.4 NodeManifest

新节点在 `package.json` 的 `baseflow` 字段中显式声明：

```json
{
  "baseflow": {
    "runtimeVersion": 1
  }
}
```

兼容规则：

- 字段不存在：作为历史节点固定按 Runtime v1 加载；
- 显式整数 `1`：按 Runtime v1 加载；
- `null`、字符串、小数、对象或其它版本：在 import 节点入口前拒绝；
- 新版官方构建工厂要求节点显式声明当前 Runtime 版本。

历史缺省版本固定为 v1，不随未来当前 Runtime 版本升级而漂移。

### 2.5 renderer 加载流程

renderer 按以下顺序加载节点：

1. 将节点来源解析为入口 URL；
2. 计算同目录 `package.json` URL；
3. 获取并解析 `baseflow` manifest；
4. 检查 `runtimeVersion`；
5. 通过检查后动态 import `index.js`；
6. 取得 default export，legacy 产物暂兼容模块对象本身；
7. 将加载错误显示在 renderer 内，不自动修复依赖或回退版本。

当前 `baseflow-node-renderer/src/App.tsx` 是 Runtime 调试壳：节点来源由 URL hash 提供，节点数据仍为固定示例。正式的节点身份、节点数据和交互协议不属于当前已落地事实。

## 3. 官方节点构建约定

### 3.1 单一契约来源

`baseflow-node-renderer/runtimeContract.js` 定义当前 Runtime 版本、历史缺省版本和五个公共模块 ID。

`baseflow-node-renderer/scripts/sharedDependencies.js` 在此基础上补充共享包版本、门面源码和输出文件名，并统一驱动：

- shared 的 lib entry；
- renderer 与官方节点的 `external`；
- renderer 的 Import Map。

### 3.2 严格 external

官方构建工厂使用以下规则：

1. 与五个入口完全相等：标记为 external；
2. 属于公共包但不是允许入口：立即抛出构建错误；
3. 其它依赖：不 external，由节点构建工具正常打包。

renderer 采用相同规则，仅允许 `@baseflow/render-react/style.css` 作为 renderer 私有实现打入自身产物。

该规则只约束官方构建工厂，不扩展为社区节点发布门禁。

### 3.3 production 构建

官方节点构建固定：

```js
define: {
  "process.env.NODE_ENV": JSON.stringify("production")
}
```

并采用 ES2022、production JSX、CSS 内联以及其它依赖默认打入节点产物的约定。

平台只保证官方配置消除静态的 `process.env.NODE_ENV` 分支，不为任意依赖提供完整 `process.env` polyfill，也不承诺扫描所有可能的环境变量访问形式。

### 3.4 shared ESM 门面

shared 在本地构建，不依赖 esm.sh 或其它在线转换服务。门面遵循：

- 当前公开 named export 与锁定版本的 production 包保持一致；
- default export 指向真实模块值，不增加嵌套 namespace；
- `react/jsx-runtime` 只提供 production 入口；
- 不输出值为 `undefined` 的伪 API；
- 当前包导出与门面导出的比较用于发现门面遗漏。

“与当前包导出一致”只验证门面构建正确，不等价于证明社区节点完整兼容，也不把 React 私有或实验性成员纳入稳定 ABI。

### 3.5 产物边界

官方节点产物为：

```text
index.js
package.json
```

CSS 内联到 `index.js` 并自行注入。节点可以自行携带本地 chunk，但平台不要求所有社区节点采用相同构建形态。

## 4. 发布与 Runtime 演进

### 4.1 本地 preview

`baseflow-preview` 是可覆盖的联合调试产物，不承担生产版本存档职责：

```text
baseflow-preview/
  renderer/
  nodes/<node-id>/
```

`baseflow-node-renderer/public/shared` 和 `baseflow-preview` 中的 shared entry 可以继续使用 `react-dom-client@19.2.8.js` 这类版本文件名。本地目录允许重建覆盖，不需要为 entry 额外生成内容 hash。

### 4.2 生产 Runtime

生产环境应将 renderer、Import Map、shared entry 和 shared chunk 作为同一个不可变 Runtime 发布单元。生产地址必须同时包含 ABI 版本和 Runtime release ID：

```text
/runtime/v1/20260827T051605Z/
```

`runtimeVersion` 表示 ABI，不能单独作为构建产物的不可变身份。同一个 Runtime v1 可以存在多个保持 ABI 兼容的实现 release。当前仓库没有生产发布脚本，本次使用 UTC 时间 `20260827T051605Z` 作为 release ID；后续每次发布按 `YYYYMMDDTHHmmssZ` 格式生成新值，已发布目录不得覆盖。

shared entry 可以继续使用依赖版本文件名，shared chunk 继续使用内容 hash。因为父级 release 路径已经不可变，不需要再给每个 entry 增加 hash，也不需要让 Import Map 读取额外的 shared 文件映射表。

每个 release 根目录直接包含 renderer 的 `index.html`、`assets/` 和 `shared/`，例如：

```text
/runtime/v1/20260827T051605Z/index.html
/runtime/v1/20260827T051605Z/assets/...
/runtime/v1/20260827T051605Z/shared/react-dom-client@19.2.8.js
```

如果提供 `/runtime/v1/current` 一类稳定别名，它只能用于查询或跳转到具体 release，不能作为长期缓存的 renderer 和 shared 资源地址。

节点 manifest 的版本选择应发生在具体 renderer 被加载之前，由父页面或轻量 Runtime bootloader 完成：

```text
节点 manifest
  -> runtimeVersion
  -> Runtime release catalog
  -> 选择 /runtime/v1/20260827T051605Z/index.html
  -> renderer 加载节点 index.js
```

当前仓库只实现单个 Runtime v1 renderer。版本选择器和生产不可变发布流程属于后续发布工程，但地址和职责边界应按上述结构保留。

一个 renderer 实例不同时运行多个 Runtime ABI。新增 Runtime 时通过选择不同 renderer 处理，而不是在同一 Import Map 中混合多套 React。

### 4.3 节点发布

正式节点应使用不可变身份：

- 节点 ID + 精确版本；或
- 节点 ID + 内容哈希。

同一个已发布身份下的 `index.js`、`package.json` 和私有 chunk 不应被覆盖。节点引用的外部 URL 不属于平台不可变性保证，开发者应自行选择精确版本或内容不可变地址。

## 5. 平台与社区责任边界

### 5.1 平台负责

- 提供约定的 Runtime 版本和五个公共入口；
- 保持官方入口的单例关系；
- 在加载前解析 manifest 并检查 Runtime 版本；
- 构建和验证官方 shared、renderer 与官方节点产物；
- 将模块加载错误尽量原样展示给开发者。

### 5.2 社区节点开发者负责

- 使用目标浏览器可加载的标准 ESM；
- 测试节点功能和第三方依赖；
- 处理 CORS、MIME、CSP、网络服务和外部 URL 稳定性；
- 避免重复 React 或未登记公共子路径带来的兼容问题；
- 验证目标部署环境中的浏览器能力和性能表现。

平台不提供能够发现所有社区节点问题的严格 verify。未来如提供诊断命令，也只能是非阻断提示，不构成平台兼容承诺。

## 6. 当前落地状态

### 6.1 已完成

- Runtime v1 五个公共入口集中定义；
- Import Map 与共享依赖表一致；
- 删除 `react/jsx-dev-runtime` 门面和 shared 入口；
- 官方节点和 renderer 使用严格 external；
- 官方节点固定 production、ES2022 和 CSS 内联；
- 新版官方构建要求 manifest 显式声明当前 Runtime；
- renderer 在 import 节点前检查 manifest；
- 历史节点缺省版本固定为 Runtime v1；
- shared 门面与当前锁定包导出进行一致性检查；
- 当前 `break` 节点已接入官方构建工厂。

### 6.2 部分完成

- renderer 目前是基于 URL hash 和固定节点数据的调试壳；
- 历史 default-export 兼容分支尚未完成发布产物盘点；
- 生产 Runtime 的 ABI + release 地址和选择边界已定义，但发布流程尚未实现。

### 6.3 后期专项

- 独立域部署、消息鉴权、安全隔离、CSP 和 Permissions Policy；
- 真实浏览器 Import Map、跨域、外部 URL、错误隔离和完整历史产物测试；
- Runtime v2 出现时的版本选择器与多版本发布工程。

## 7. 决策结论

- Runtime ABI 只定义加载协议、五个公共入口及其公开稳定能力；
- 构建工具不属于 ABI，以最终标准 ESM 产物为准；
- 平台只做加载所需的最小检查，不扫描社区节点完整依赖图；
- 社区节点可以自行使用本地 chunk 或完整 HTTP(S) ESM URL；
- 社区节点的完整运行兼容性由开发者在目标环境中自行测试；
- 当前 renderer 是 Runtime v1 调试壳，不能表述为完整产品交互协议；
- 不兼容的 Runtime 变化通过新的 ABI 目录解决；兼容更新通过新的不可变 release 目录发布，不覆盖旧 Runtime，也不要求历史节点重新构建。
