{
  "name": "baseflow-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "type:check": "tsc --noEmit",
    "lint": "biome check . && stylelint \"./src/**/*.scss\"",
    "lint:fix": "biome check --write . && stylelint \"./src/**/*.scss\" --fix",
    "mock:generate": "node ./scripts/generateMock.js",
    "monaco:copy": "node ./scripts/copyMonaco.js",
    "preview:clean": "node ./scripts/cleanPreview.js",
    "prebuild": "npm run monaco:copy && npm run mock:generate && npm run preview:clean",
    "predev": "npm run monaco:copy && npm run mock:generate",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "peerDependencies": {
    "react": "*",
    "react-dom": "*",
    "axios": "*",
    "antd": "*",
    "lucide-react": "*",
    "dayjs": "*",
    "@baseflow/flow-react": "*"
  },
  "dependencies": {
    "reactflow": "^11.11.4"
  }
}
