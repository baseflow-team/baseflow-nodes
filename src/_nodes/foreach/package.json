{
  "name": "@baseflow-nodes/foreach",
  "version": "0.0.1",
  "private": true,
  "baseflow": {
    "type": "Loop",
    "name": "Foreach循环",
    "icon": "",
    "desc": "Foreach循环：通过指定一个迭代源来循环执行子节点"
  },
  "executor": {
    "node": "@baseflow-executors/foreach@0.0.1"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "rollup -c ../../../scripts/rollup.config.js",
    "publish": "npm publish ../../../public/nodes/foreach"
  }
}
